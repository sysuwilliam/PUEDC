# 系统理解

## 模块架构图

## 最新系统框架图：SBC集成视觉与云台控制方案

### 总体架构

```mermaid
flowchart TB
    subgraph SBC["SBC一体板：MPU + STM32U585"]
        CAM["大摄像头<br/>卷帘快门"]
        MPU["MPU / Linux侧<br/>视觉流程<br/>图像采集、识别、目标测量"]
        U585["板载 STM32U585<br/>云台MCU<br/>运行SimpleFOC程序"]

        CAM --> MPU
        MPU <-->|板内UART协议<br/>目标坐标 / 目标误差 / 视觉状态| U585
    end

    subgraph GIMBAL["两轴云台系统"]
        AS_YAW["Yaw轴编码器<br/>当前AS5600<br/>若上TMC9660可改MT6701"]
        AS_PITCH["Pitch轴AS5600"]
        DRV_YAW["Yaw轴SimpleFOC驱动板<br/>或TMC9660备选方案"]
        DRV_PITCH["Pitch轴SimpleFOC驱动板"]
        M_YAW["Yaw轴无刷电机<br/>当前2804<br/>可能更换更大电机"]
        M_PITCH["Pitch轴2804无刷电机"]
        MECH["云台机械结构<br/>相机 / 激光 / Pitch机构<br/>必要时导电滑环"]

        AS_YAW --> U585
        AS_PITCH --> U585
        U585 --> DRV_YAW --> M_YAW --> MECH
        U585 --> DRV_PITCH --> M_PITCH --> MECH
    end

    subgraph CHASSIS["底盘系统"]
        G3507["G3507底盘主控"]
        GRAY["八路灰度传感器"]
        IMU["底盘IMU"]
        MOTOR_DRV["底盘电机驱动板"]
        WHEELS["四个520编码器电机"]

        GRAY --> G3507
        IMU --> G3507
        G3507 --> MOTOR_DRV --> WHEELS
        WHEELS -->|编码器反馈| G3507
    end

    subgraph POWER["电源系统"]
        BAT["电池"]
        BUCK["降压模块<br/>5V / 3V3 / 电机电源"]
    end

    BAT --> BUCK
    BUCK --> SBC
    BUCK --> G3507
    BUCK --> MOTOR_DRV
    BUCK --> DRV_YAW
    BUCK --> DRV_PITCH

    SBC <-->|统一UART协议<br/>视觉状态 / 云台状态 / 底盘命令 / 心跳| G3507
```

### 备选方案

#### K230视觉 + 外置STM32U5云台MCU

```mermaid
flowchart TB
    subgraph VISION["K230视觉模块"]
        K_CAM["K230摄像头"]
        K230["K230核心板<br/>图像采集 / 识别 / 目标测量<br/>输出结构化视觉数据"]

        K_CAM --> K230
    end

    subgraph U5_GIMBAL["外置STM32U5云台控制板"]
        U5["STM32U5开发板<br/>云台MCU<br/>运行SimpleFOC程序"]
        AS_YAW["Yaw轴AS5600<br/>或后续MT6701"]
        AS_PITCH["Pitch轴AS5600"]
        DRV_YAW["Yaw轴SimpleFOC驱动板"]
        DRV_PITCH["Pitch轴SimpleFOC驱动板"]
        M_YAW["Yaw轴2804无刷电机<br/>可能后续更换更大电机"]
        M_PITCH["Pitch轴2804无刷电机"]

        AS_YAW --> U5
        AS_PITCH --> U5
        U5 --> DRV_YAW --> M_YAW
        U5 --> DRV_PITCH --> M_PITCH
    end

    subgraph CHASSIS_ALT["底盘系统"]
        G3507_ALT["G3507底盘主控"]
        MOTOR_ALT["底盘电机 / 灰度 / IMU"]
        G3507_ALT --> MOTOR_ALT
    end

    K230 -->|UART<br/>目标坐标/目标误差/视觉状态| U5
    U5 <-->|统一UART协议<br/>云台状态 / 底盘协同 / 心跳| G3507_ALT
    K230 -.->|可选：视觉数据同步| G3507_ALT
```

#### 备选方案定位

该方案的作用：

* 快速验证视觉输出数据是否可用；
* 快速验证视觉误差到云台动作的控制链路；
* 在SBC方案尚未完全确认前，为云台和底盘联调提供可运行平台；
* 协助制定统一UART协议；
* 让底盘可以提前接收“视觉目标状态 / 云台状态”等数据进行联调；
* 若SBC板载STM32U585开发或SimpleFOC适配受阻，可临时回退到该方案。

该方案的缺点：

* K230 + 外置STM32U5开发板体积更大；
* 线缆和供电更复杂；
* 最终上车空间压力较大；
* 不是最终集成优先方案，更适合作为调试过渡方案。

### 视觉与云台控制数据流

```mermaid
flowchart LR
    A["摄像头图像"] --> B["MPU视觉流程"]
    B --> C["结构化目标数据<br/>target_found<br/>error_x / error_y<br/>confidence<br/>timestamp"]
    C --> D["板内UART发送给STM32U585"]
    D --> E["云台控制逻辑<br/>像素误差 → 目标角度 / 目标角速度"]
    E --> F["SimpleFOC<br/>位置环 / 速度环 / FOC"]
    F --> G["SimpleFOC驱动板"]
    G --> H["Yaw / Pitch无刷电机"]
    H --> I["AS5600 / MT6701反馈"]
    I --> F

    C --> J["必要时同步给G3507"]
    J --> K["底盘协同<br/>目标超出云台范围时辅助转向"]
```

### 底盘控制数据流

(还没加入mpu6500)

```mermaid
flowchart LR
    A["八路灰度传感器"] --> B["灰度误差计算"]
    B --> C["循迹PID / 状态机"]
    C --> D["目标速度v<br/>目标角速度ω"]
    D --> E["左右轮目标速度"]
    E --> F["四轮速度闭环"]
    F --> G["底盘电机驱动"]
    G --> H["四个520编码器电机"]
    H --> I["编码器反馈"]
    I --> F

    J["SBC / 云台状态"] --> C
    C --> K["底盘状态反馈"]
    K --> J
```

### Yaw轴TMC9660备选路线

```mermaid
flowchart TB
    TOP["上层控制器<br/>SBC板载STM32U585 / 底盘主控 / 调试上位机"]
    TMC["TMC9660<br/>硬件FOC伺服控制器<br/>位置环 / 速度环 / 电流环 / SVPWM"]
    ENC["MT6701编码器<br/>Yaw轴位置反馈<br/>优先使用ABZ/ABN等适配接口"]
    POWER["外部MOS功率级 / 评估板功率级"]
    MOTOR["更大Yaw轴无刷电机"]
    LOAD["Yaw轴负载<br/>Pitch机构 / 相机 / 线缆 / 激光 / 导电滑环"]

    TOP -->|UART / SPI<br/>目标角度或目标速度| TMC
    ENC -->|位置反馈| TMC
    TMC --> POWER
    POWER --> MOTOR
    MOTOR --> LOAD
    LOAD --> ENC
```

### 待验证问题

```text
SBC方案：
[ ] MPU侧视觉结果的输出字段、频率、延迟
[ ] MPU侧与STM32U585之间的板内UART协议
[ ] STM32U585是否方便运行SimpleFOC或厂商等效电机控制程序
[ ] STM32U585的PWM / I2C资源是否足够控制两轴云台
[ ] 两个AS5600的I2C地址冲突如何解决
[ ] SBC与G3507之间的统一UART协议帧格式
...

云台方案：
[ ] 当前2804 Yaw轴转矩是否足够
[ ] Pitch机构、相机、线缆对Yaw轴转动惯量的影响
[ ] 是否需要更大Yaw轴电机
[ ] 若Yaw轴换大电机，TMC9660 + MT6701方案是否可行
[ ] 若使用导电滑环，电源线、信号线、编码器线如何分配
...

底盘方案：
[ ] G3507底盘主控与SBC之间的UART通信频率
[ ] 底盘状态反馈字段
[ ] 云台到限位后，底盘是否需要辅助转向
[ ] 视觉目标丢失时，底盘和云台如何进入安全状态\
...
```

## 代码接口统一

```
通信协议格式；
数据单位，比如角度用 degree 还是 rad，速度用 rpm 还是 m/s；
坐标正方向，比如 error_x > 0 到底表示目标在右边还是左边；
状态枚举，比如 MODE_TRACK = 2；
故障码；
对外接口函数命名
```

## 模块任务

### 视觉

* 硬件与基础环境
* 图像预处理
* 识别与测量
* 视觉决策
* 通信接口
* 实际环境测试

### 云台

* 云台机械结构
* 无刷电机驱动
* 角度反馈
* 控制环
* 标定与保护

### 底盘

* 电机与驱动硬件
* 单电机基础控制
* 速度闭环
* 底盘运动封装
* 循迹控制
* IMU与姿态控制
* 故障和安全
